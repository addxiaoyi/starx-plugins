<!-- Generated by Trae Work -->
# StarX Plugins

**Minecraft 代理 + 子服 All-in-One 插件套件** · Velocity 代理端 28 个模块 + Paper 子服端 10 个模块

[![CI](https://github.com/addxiaoyi/starx-plugins/actions/workflows/ci.yml/badge.svg)](https://github.com/addxiaoyi/starx-plugins/actions)
[![Java](https://img.shields.io/badge/Java-21-orange)](https://adoptium.net/)
[![Velocity](https://img.shields.io/badge/Velocity-3.5.x-blue)](https://papermc.io/software/velocity)
[![Minecraft](https://img.shields.io/badge/Minecraft-1.21.4+-brightgreen)](https://www.minecraft.net/)
[![License](https://img.shields.io/badge/License-MIT-blue)](LICENSE)

---

## 目录

- [安装](#安装)
- [命令列表](#命令列表)
- [权限列表](#权限列表)
- [功能详解](#功能详解)
- [配置](#配置)
- [HTTP API](#http-api)
- [升级](#升级)
- [开发](#开发)

---

## 安装

### 环境要求

- **Java 21** 或更高版本
- **Velocity 3.5.x** 代理端
- **Paper 1.21.4+** 子服端（支持 Folia）

### 安装步骤

1. 从 [Releases](https://github.com/addxiaoyi/starx-plugins/releases) 下载最新版本：
   - `starx-velocity-*-all.jar` — Velocity 端
   - `starx-paper-*-all.jar` — Paper 端

2. **Velocity 端**：将 `starx-velocity-*-all.jar` 放入 Velocity 的 `plugins/` 目录

3. **Paper 端**：将 `starx-paper-*-all.jar` 放入每个子服的 `plugins/` 目录

4. 确保 `server.properties` 配置正确：
   ```properties
   online-mode=false
   velocity.enabled=true
   velocity.secret=<与 velocity.toml 一致>
   ```

5. 重启服务，StarX 会自动生成配置文件 `plugins/starx/config.yml`

6. 按需编辑配置文件，重启生效

---

## 命令列表

### 玩家命令

> 登录/注册无需命令，首次进入时直接在聊天框输入密码即可自动注册，之后输入密码即可登录。

| 命令 | 说明 |
|------|------|
| `/hub` / `/lobby` | 返回大厅服务器 |
| `/list` | 查看所有在线玩家及所在子服 |
| `/ping [玩家]` | 查看自己或他人的延迟 |
| `/skin` | 刷新皮肤 |
| `/2fa` | 查看/管理二步验证（enable/disable/status） |

### 管理员命令

| 命令 | 说明 | 权限 |
|------|------|------|
| `/glist` | 全局服务器列表，各子服及在线玩家 | `starx.commands.glist` |
| `/find <玩家>` | 查找玩家所在服务器 | `starx.commands.find` |
| `/send <玩家> <服务器>` | 将玩家传送到指定服务器 | `starx.commands.send` |
| `/alert <消息>` | 全服广播 | `starx.commands.alert` |
| `/kickall <服务器> [原因]` | 踢出指定服务器的所有玩家 | `starx.commands.kickall` |
| `/maintenance <on\|off>` | 开关维护模式 | `starx.maintenance.bypass` |
| `/starx info` | 代理状态（玩家数、内存、运行时间） | 无 |
| `/starx servers` | 所有子服在线状态 | 无 |
| `/starx uptime` | 代理运行时间 | 无 |
| `/starx anticheat` | 反作弊统计摘要 | 无 |
| `/starx anticheat player <玩家>` | 查看玩家违规详情 | 无 |
| `/starx anticheat clear [玩家]` | 清除反作弊数据 | 无 |

---

## 权限列表

| 权限节点 | 说明 |
|----------|------|
| `starx.commands.glist` | 使用 `/glist` 命令 |
| `starx.commands.find` | 使用 `/find` 命令 |
| `starx.commands.send` | 使用 `/send` 命令 |
| `starx.commands.alert` | 使用 `/alert` 命令 |
| `starx.commands.ping` | 使用 `/ping` 命令 |
| `starx.commands.kickall` | 使用 `/kickall` 命令 |
| `starx.kickall.bypass` | 被 `/kickall` 时不会被踢出 |
| `starx.maintenance.bypass` | 维护模式时仍可登录 |
| `starx.anticheat.bypass` | 绕过反作弊检测 |
| `starx.crashfix.bypass` | 绕过崩溃拦截限制 |

---

## 功能详解

### 账号系统

完整的登录、注册、二步验证体系。

- 玩家首次进入时直接在聊天框输入密码自动注册，之后输入密码即可登录
- 二步验证（TOTP）为可选功能，使用 `/2fa enable <密码>` 开启，`/2fa disable <密码>` 关闭
- 开启后登录时自动要求输入验证码，支持 Google Authenticator 等 App
- 支持正版在线验证 + 离线模式双路由
- 支持第三方 Yggdrasil 验证服务器对接
- Mojang → Microsoft 账户迁移检测与提示

### 维护模式

开启后只有拥有 `starx.maintenance.bypass` 权限的玩家可以进入，其他玩家看到维护提示。

```
/maintenance on     # 开启维护
/maintenance off    # 关闭维护
```

### 排队系统

满服时自动排队，支持 VIP 优先级跳过。玩家在排队期间停留在 Limbo 虚拟大厅，有空位时自动进入。

### 动态 MOTD

服务器列表显示的 MOTD 根据在线人数、服务器状态动态变化，支持自定义图标和文本。

### 全局聊天

不同子服玩家可以互相聊天，消息跨服同步显示。

### 皮肤系统

配合子服的 SkinsRestorer 插件，玩家更换皮肤后自动同步到所有子服。`/skin` 命令可手动刷新。

### 断线重连

玩家意外断线后，在配置时间内重连会自动回到原来的服务器，不会丢失位置。

### 反作弊

自动检测以下异常行为：

- 移动异常（飞行、加速、瞬移）
- 战斗加速
- 数据包速率异常
- 破坏/放置速度异常

`/starx anticheat player <玩家>` 可查看具体违规记录。

### 机器人防护

检测并拦截可疑机器人连接，支持：
- 连接速率限制
- IP 黑名单
- 可选验证码

### 崩溃修复

自动拦截可能导致服务器崩溃的操作：
- 超大书本（超过配置页数限制）
- 异常 NBT 数据
- 危险命令
- 非法数据包

### 基岩版兼容

允许基岩版（BE）玩家通过 Floodgate 加入 Java 版服务器，支持 RakNet 协议。

### Forge 兼容

支持 Forge 客户端通过代理连接，处理 FML2 握手协议。

### QQ 机器人

对接 QQ 群机器人，支持：
- 群内执行服务器命令
- 查看服务器状态
- 接收事件通知

### 增强代理命令

一组强大的代理管理命令：`/glist` 全局服务器列表、`/find` 查找玩家、`/send` 传送玩家、`/alert` 全服公告、`/ping` 延迟查看、`/kickall` 批量踢出。

### 文件清理

定时清理过期日志和缓存文件，可按天数和文件大小设置规则。

### 社交功能

好友系统、公会、跨服私聊（需 Velocity 端和 Paper 端同时启用）。

---

## 配置

首次启动后，配置文件自动生成在 `plugins/starx/config.yml`。以下为完整配置选项：

### 数据库

```yaml
database:
  # 可选: h2, mysql, postgresql
  type: "h2"
  host: ""
  port: 3306
  database: "starx"
  username: "starx"
  password: ""
  url: ""                     # 自定义 JDBC URL（留空则用以上字段自动拼接）
  pool-max-size: 10
  connection-timeout-ms: 30000
```

**MySQL 示例**：
```yaml
database:
  type: "mysql"
  host: "localhost"
  port: 3306
  database: "starx"
  username: "starx"
  password: "your_password"
```

### HTTP API 与 Webhook

```yaml
api-key: ""                   # 网站后端调用插件的 API 密钥（务必修改）

http:
  bind: "127.0.0.1"          # 监听地址
  port: 8788                  # 监听端口

webhook:
  url: ""                     # 网站 Webhook 回调地址
  secret: ""                  # HMAC 签名密钥
```

### 模块开关

```yaml
modules:
  auth:
    enabled: true
  auth.yggdrasil:
    enabled: true
  auth.uniauth:
    enabled: false
  auth.floodgate:
    enabled: true
  auth.tab:
    enabled: true
  auth.migration:
    enabled: false
  skin-bridge:
    enabled: true
  messaging:
    enabled: true
  proxytools.maintenance:
    enabled: true
  proxytools.motd:
    enabled: true
  proxytools.chat:
    enabled: true
  proxytools.redirect:
    enabled: true
  proxytools.queue:
    enabled: true
  proxytools.limbo:
    enabled: true
  proxytools.reconnect:
    enabled: true
  proxytools.info:
    enabled: true
  proxytools.forge:
    enabled: false
  proxytools.raknet:
    enabled: false
  proxytools.online:
    enabled: true
  proxytools.enhanced:
    enabled: true
  proxytools.filecleaner:
    enabled: false
  security.bot:
    enabled: true
  security.crash:
    enabled: true
  security.risk:
    enabled: true
  security.anticheat:
    enabled: true
  integrations.qq:
    enabled: false
  integrations.plan:
    enabled: false
  integrations.mapmod:
    enabled: false
  integrations.social:
    enabled: false
```

---

## HTTP API

StarX 提供 REST API 用于外部管理，默认端口 **8788**，使用 `X-API-Key` 请求头或 HMAC 签名鉴权。

### VLA 管理端点

| 端点 | 方法 | 说明 |
|------|------|------|
| `/v1/user/exists?username=<name>` | GET | 查询用户是否存在 |
| `/v1/user/detail?username=<name>` | GET | 查询用户详情（含 totpEnabled、lastLoginAt） |
| `/v1/admin/reset-password` | POST | 重置玩家密码 |
| `/v1/admin/bind-email` | POST | 绑定玩家邮箱 |
| `/v1/admin/delete-user` | POST | 删除玩家账户 |
| `/v1/ban?username=<name>` | GET | 查询封禁状态 |
| `/v1/admin/ban` | POST | 封禁/解封玩家 |
| `/v1/admin/kick-online` | POST | 踢出在线玩家 |
| `/v1/admin/link-external-user` | POST | 关联外部网站用户 ID |
| `/v1/admin/skin-refresh` | POST | 请求刷新玩家皮肤 |

### 请求示例

```bash
# 查询用户是否存在
curl http://127.0.0.1:8788/v1/user/exists?username=Steve \
  -H "X-API-Key: <your-api-key>"

# 重置密码
curl -X POST http://127.0.0.1:8788/v1/admin/reset-password \
  -H "X-API-Key: <your-api-key>" \
  -H "Content-Type: application/json" \
  -d '{"username": "Steve", "password": "new_password"}'
```

### 端点列表

| 端点 | 方法 | 用途 |
|------|------|------|
| `/api/v2/plugin/status` | GET | 代理状态查询 |
| `/api/v2/plugin/online` | GET | 在线玩家列表 |
| `/api/v2/plugin/kick` | POST | 踢出玩家 |
| `/api/v2/plugin/ban` | POST | 封禁玩家 |
| `/api/v2/plugin/alert` | POST | 全服公告 |
| `/api/v2/plugin/user` | GET | 查询用户信息 |
| `/api/v2/plugin/user/delete` | DELETE | 删除用户 |
| `/api/v2/plugin/user/password` | POST | 重置用户密码 |
| `/api/v2/plugin/user/email` | POST | 绑定邮箱 |
| `/api/v2/plugin/user/link` | POST | 关联外部账户 |
| `/api/v2/plugin/skin/refresh` | POST | 刷新皮肤缓存 |

---

## 升级

1. 备份 `plugins/starx/config.yml` 和数据库文件
2. 停止 Velocity 和所有子服
3. 替换旧的 jar 文件为新版本
4. 启动服务，数据库迁移自动执行

---

## 故障排查

| 问题 | 可能原因 | 解决方案 |
|------|----------|----------|
| 玩家无法登录 | Velocity 与子服 secret 不一致 | 核对 `velocity.toml` 与 `server.properties` |
| HTTP API 无法访问 | 端口被占用 | 检查端口占用，修改配置 |
| 皮肤未同步 | SkinsRestorer 未安装 | 确认子服已安装 SkinsRestorer |
| 数据库连接失败 | URL 或凭据错误 | 检查数据库配置 |
| 模块未生效 | 模块配置为 disabled | 检查 `config.yml` 中 `modules` 节点 |

---

## 开发

### 从源码构建

```bash
# 克隆仓库
git clone https://github.com/addxiaoyi/starx-plugins.git
cd starx-plugins

# 运行测试
./gradlew test

# 构建
./gradlew :starx-velocity:shadowJar :starx-paper:shadowJar
```

产物位于：
- `starx-velocity/build/libs/starx-velocity-*-all.jar`
- `starx-paper/build/libs/starx-paper-*-all.jar`

### 代码规范

```bash
# 格式化 (Google Java Format)
./gradlew spotlessApply

# 检查格式
./gradlew spotlessJavaCheck
```

### 技术栈

| 技术 | 用途 |
|------|------|
| Java 21 | 开发语言 |
| Gradle 8.14 + Kotlin DSL | 构建系统 |
| Velocity API 3.5.x | 代理端 |
| Paper API 1.21.4 | 子服端 |
| HikariCP + JDBI + Flyway | 数据库 |
| H2 / MySQL / PostgreSQL | 存储后端 |
| JUnit 5 + Mockito | 测试框架 |
| Google Java Format | 代码格式化 |

---

## 许可证

[MIT](LICENSE)